@model IEnumerable<BS.DMO.ViewModels.Setup.BANK_BRANCH_VM>
@{
    ViewData["Title"] = "Bank Branch";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card card-primary card-outline">
    <div class="card-header">
        <h3 class="card-title">Find your @ViewData["Title"]</h3>
        <div class="card-tools">
            <a class="btn btn-primary btn-xs" asp-action="Create"><i class="fa fa-plus"></i> Create New</a>
        </div>
    </div>

    <div class="card-body">
        <table class="table table-hover table-sm">
            <thead>
                <tr>
                    <th>Bank</th>
                    <th>Branch</th>
                    <th>Short Name</th>
                    <th>Address</th>
                    <th>Route</th>
                    <th>Swift Code</th>
                    <th>#</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.BANK_NAME)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BRANCH_NAME)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SHORT_NAME)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BRANCH_ADDRESS)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ROUTE_NO)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SWIFT_CODE)
                        </td>
                        <td>
                            <a class="btn btn-primary btn-xs" asp-action="Edit" asp-route-id="@item.ID"><i class="fa fa-edit"></i> Edit</a>
                            <button type="button" class="btn btn-danger btn-xs delete-confirm" data-url="/Setup/BankBranch/Delete/@item.ID"><i class="fa fa-trash"></i> Delete</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>